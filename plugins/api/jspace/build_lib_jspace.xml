<?xml version="1.0" encoding="UTF-8"?>
<project name="API - JSpace Plugin" default="" basedir=".">
	<property name="dest.plugins.jspace" value="${dest.plugins}/api"/>

	<target name="update" description="">
		<echo message="Deploying API - JSpace Plugin to Joomla! environment."/>
		<copy todir="${dest.plugins.jspace}/jspace">
			<fileset dir="${src}" defaultexcludes="true">
				<include name="**"/>
				<exclude name="**/language/**"/>
				<exclude name="**/build*.xml"/>
			</fileset>
		</copy>
		<copy todir="${dest.administrator.languages}">
			<fileset dir="${src}/language" defaultexcludes="true">
				<include name="**/**"/>
			</fileset>
		</copy>
	</target>

	<target name="package" description="Creates a API - JSpace Plugin package.">
		<echo message="Packaging API - JSpace plugin."/>
		<zip destfile="${package.dest}/plg_api_jspace.zip"
		       basedir="${src}"
		       update="true"
		       excludes="build*.xml"/>
	</target>
</project>