<?xml version="1.0" encoding="UTF-8"?>
<project name="JSpace Content JOAI Plugin" default="" basedir=".">
	<property name="dest.plugins.content" value="${dest.plugins}/content"/>
	
	<target name="update" description="">
		<echo message="Deploying JSpace Content JOAI Plugin to Joomla! environment."/>
		<copy todir="${dest.plugins.content}/joai">
			<fileset dir="${src}" defaultexcludes="true">
				<include name="**"/>
				<exclude name="**/language/**"/>
				<exclude name="**/build*.xml"/>
			</fileset>
		</copy>
		<copy todir="${dest.administrator.languages}">
			<fileset dir="${src}/language" defaultexcludes="true">
				<include name="**/**"/>
			</fileset>
		</copy>
	</target>

	<target name="package" description="Creates a Content JOAI Plugin package.">
		<echo message="Packaging Content JOAI plugin."/>
		<zip destfile="${package.dest}/plg_content_joai.zip"
		       basedir="${src}"
		       update="true"
		       excludes="build*.xml"/>
	</target>
</project>