<?xml version="1.0" encoding="UTF-8"?>
<project name="Content - OAI Plugin" default="" basedir=".">
	<property name="dest.plugins.jspace" value="${dest.plugins}/content"/>
	
	<target name="update" description="">
		<echo message="Deploying JSpace Cli OAI Plugin to Joomla! environment."/>
		<copy todir="${dest.plugins.jspace}/oai">
			<fileset dir="${src}" defaultexcludes="true">
				<include name="**"/>
				<exclude name="**/language/**"/>
				<exclude name="**/build*.xml"/>
			</fileset>
		</copy>
		<copy todir="${dest.administrator.languages}">
			<fileset dir="${src}/language" defaultexcludes="true">
				<include name="**/**"/>
			</fileset>
		</copy>
	</target>

	<target name="package" description="Creates a Content - OAI Plugin package.">
		<echo message="Packaging Content - OAI plugin."/>
		<zip destfile="${package.dest}/plg_content_oai.zip"
		       basedir="${src}"
		       update="true"
		       excludes="build*.xml"/>
	</target>
</project>