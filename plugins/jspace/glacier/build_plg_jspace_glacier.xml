<?xml version="1.0" encoding="UTF-8"?>
<project name="JSpace Glacier Plugin" default="" basedir=".">
	<property name="dest.plugins.jspace" value="${dest.plugins}/jspace"/>
	
	<target name="update" description="">
		<echo message="Deploying JSpace Glacier Plugin to Joomla! environment."/>
		<copy todir="${dest.plugins.jspace}/glacier">
			<fileset dir="${src}" defaultexcludes="true">
				<include name="**"/>
				<exclude name="**/language/**"/>
				<exclude name="**/build*.xml"/>
			</fileset>
		</copy>
		<copy todir="${dest.administrator.languages}">
			<fileset dir="${src}/language" defaultexcludes="true">
				<include name="**/**"/>
			</fileset>
		</copy>
	</target>

	<target name="package" description="Creates a Glacier Plugin package.">
		<echo message="Packaging JSpace Glacier plugin."/>
		<zip destfile="${package.dest}/plg_jspace_glacier.zip"
		       basedir="${src}"
		       update="true"
		       excludes="build*.xml"/>
	</target>
</project>